<UserControl x:Class="StudentGradesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:School_Management_System"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20,18,20,22">
            <Border Style="{StaticResource DashboardCardBorderStyle}">
                <Grid Margin="18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Grades"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource DashboardTextPrimaryBrush}"/>
                        <TextBlock x:Name="GradesStudentIdTextBlock"
                                   Text=""
                                   Margin="0,4,0,0"
                                   FontSize="13"
                                   Foreground="{StaticResource DashboardTextMutedBrush}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Margin="0,0,8,0"
                                Padding="12,8"
                                CornerRadius="8"
                                Background="#F4F8FD"
                                BorderBrush="{StaticResource DashboardBorderBrush}"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Current GWA" FontSize="11" Foreground="{StaticResource DashboardTextMutedBrush}"/>
                                <TextBlock Text="0" FontSize="17" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                        <Border Padding="12,8"
                                CornerRadius="8"
                                Background="#F4F8FD"
                                BorderBrush="{StaticResource DashboardBorderBrush}"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="Completed Units" FontSize="11" Foreground="{StaticResource DashboardTextMutedBrush}"/>
                                <TextBlock Text="0" FontSize="17" FontWeight="Bold"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource DashboardCardBorderStyle}" Margin="0,18,0,0">
                <TabControl Margin="18"
                            Background="Transparent"
                            BorderThickness="0">
                    <TabItem Header="Current">
                        <local:StudentCurrentGradesTab x:Name="CurrentGradesTabView"
                                                       Margin="0,14,0,0"/>
                    </TabItem>

                    <TabItem Header="Breakdown">
                        <local:StudentGradeBreakdownTab Margin="0,14,0,0"/>
                    </TabItem>

                    <TabItem Header="History">
                        <local:StudentGradeHistoryTab Margin="0,14,0,0"/>
                    </TabItem>
                </TabControl>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
